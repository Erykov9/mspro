export const getLangData = (state => state.langData);

const langDataReducer = (statePart = [], action) => {
  switch(action.type) {
    default:
      return statePart
  }
};

export default langDataReducer;