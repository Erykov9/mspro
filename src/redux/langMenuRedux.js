export const getLangMenu = (state => state.menu);

const langMenuReducer = (statePart = [], action) => {
  switch(action.type) {
    default:
      return statePart
  }
};

export default langMenuReducer;