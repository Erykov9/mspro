export const getOffertMenu = (state => state.offert);

const offertReducer = (statePart = [], action) => {
  switch(action.type) {
    default:
      return statePart
  }
};

export default offertReducer;